{
  "name": "js_fs_readfile_info",
  "version": "0.0.2",
  "description": "promisify fs readfile and return additional info on file",
  "author": "lynzt",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "pretest": "docker build -t node/read_file_tests .",
    "run": "docker run -p 4000:3000 node:6-onbuild --rm -t -i -v $PWD:/usr/src/app",
    "test": "mocha --recursive"
  },
  "dependencies": {
    "bluebird": "~3.4.6"
  },
  "devDependencies": {
    "chai": "~3.0.0",
    "mocha": "~2.2.5"
  }
}
